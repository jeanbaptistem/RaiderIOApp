<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".MainActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/app_bar_expanded_height"
        android:fitsSystemWindows="true"
        android:theme="@style/Theme.RaiderIoApp.AppBarOverlay">

        <fr.jbme.raiderioapp.components.CustomCollapsingToolbarLayout
            android:id="@+id/toolbar_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fitsSystemWindows="true"
            app:contentScrim="@color/colorPrimary"
            app:layout_scrollFlags="scroll|exitUntilCollapsed"
            app:scrimAnimationDuration="200"
            app:statusBarScrim="@color/colorPrimaryDark"
            app:titleEnabled="false"
            app:toolbarId="@+id/toolbar">

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/charInfoCardView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|start"
                android:layout_marginStart="8dp"
                android:layout_marginBottom="8dp"
                app:cardBackgroundColor="@color/cardBackgroundFaded"
                app:cardCornerRadius="8dp"
                app:cardElevation="0dp"
                app:cardMaxElevation="0dp"
                app:contentPadding="6dp"
                app:layout_collapseMode="parallax"
                app:layout_collapseParallaxMultiplier="0.5">

                <androidx.constraintlayout.widget.ConstraintLayout
                    android:id="@+id/toolbarContentLayout"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:fitsSystemWindows="true">

                    <TextView
                        android:id="@+id/charTitleTextView"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAlignment="viewStart"
                        android:textSize="18sp"
                        app:layout_constraintBottom_toTopOf="@+id/charNameTextView"
                        app:layout_constraintStart_toStartOf="@+id/charNameTextView"
                        tools:text="Long-voyant" />

                    <TextView
                        android:id="@+id/charNameTextView"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAlignment="viewStart"
                        android:textSize="24sp"
                        android:textStyle="bold"
                        app:layout_constraintBottom_toTopOf="@+id/charClassTextView"
                        app:layout_constraintStart_toStartOf="@+id/charClassTextView"
                        tools:text="Kagdush" />

                    <TextView
                        android:id="@+id/charRealmTextView"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="6dp"
                        android:textAlignment="viewStart"
                        android:textSize="18sp"
                        app:layout_constraintBottom_toBottomOf="@+id/charNameTextView"
                        app:layout_constraintStart_toEndOf="@+id/charNameTextView"
                        tools:text="Ysondre" />

                    <TextView
                        android:id="@+id/charClassTextView"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAlignment="viewStart"
                        android:textSize="14sp"
                        app:layout_constraintBottom_toBottomOf="parent"
                        app:layout_constraintStart_toStartOf="parent"
                        tools:text="Shaman" />

                    <TextView
                        android:id="@+id/charSpectextView"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:textAlignment="viewStart"
                        android:textSize="14sp"
                        app:layout_constraintBottom_toBottomOf="@+id/charClassTextView"
                        app:layout_constraintStart_toEndOf="@+id/charClassTextView"
                        app:layout_constraintTop_toTopOf="@+id/charClassTextView"
                        tools:text="Enhancement" />

                </androidx.constraintlayout.widget.ConstraintLayout>
            </com.google.android.material.card.MaterialCardView>

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                app:layout_collapseMode="pin"
                app:popupTheme="@style/Theme.RaiderIoApp.PopupOverlay" />

        </fr.jbme.raiderioapp.components.CustomCollapsingToolbarLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <include layout="@layout/content_main" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>